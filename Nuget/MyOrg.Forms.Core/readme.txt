This one will be packed as a nuget package and being shared across our Xamarin apps.

This will contains all of reusable components, helpers, which help to make consistent UI and boost faster development.

The .Droid and .iOS with corresponding package will have be the support packages for this one (Renderers, Effects which is platform specific)